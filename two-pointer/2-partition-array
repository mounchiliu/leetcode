https://www.lintcode.com/problem/partition-array/
(快排、快选的灵魂所在)
//与quick sort不同之处：
//quickSort 等于的情况左右两边都可以放，而这一题规定，等于只放在右边
class Solution {
public:
    /**
     * @param nums: The integer array you should partition
     * @param k: An integer
     * @return: The index after partition
     */
    int partitionArray(vector<int> &nums, int k) {
        // write your code here
        if(nums.size() == 0)
            return 0;
            
        int left = 0, right = nums.size() - 1;
        while(left <= right){
            while(left <= right && nums[left] < k){//扫描，在左边找不符合要求的，即大于等于的情况
                left++;
            }
            while(left <= right && nums[right] >= k){ //扫描，在右边找不符合要求的，即小于的情况
                right--;
            }
            
            if(left <= right){
                swap(nums[left], nums[right]);
                left++;
                right--;
            }
        }
        return left;//返回数组划分的位置，即数组中第一个位置 i，满足 nums[i] 大于等于 k。
    }
};
